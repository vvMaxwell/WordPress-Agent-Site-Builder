{
  "name": "wordpress-agent-site-builder",
  "version": "0.1.0",
  "description": "AI-agent framework for building editable WordPress sites, with Themify Ultra as the preferred workflow.",
  "private": false,
  "type": "module",
  "scripts": {
    "generate": "node automation/scripts/generate-plan.mjs",
    "test": "playwright test",
    "playwright:install": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  }
}
