{
  "name": "wordpress-agent-site-builder",
  "version": "0.1.0",
  "description": "Helps AI coding agents build editable WordPress sites. Works best with Themify Ultra.",
  "private": false,
  "type": "module",
  "scripts": {
    "generate": "node automation/scripts/generate-plan.mjs",
    "test": "playwright test",
    "playwright:install": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  }
}
